<%-include('includes/head.ejs')%>
<h1>Users</h1>
    <%if (hasusers) { %>
        <ul>
            <% users.forEach((user)=>{ %>
                <li><%=user.name%></li>
            <%})%>
        </ul>
   <% } else { %>
            <h1>No Users Found!!</h1>
    <%}%>
<%-include('includes/end.ejs')%>